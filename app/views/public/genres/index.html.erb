<div class="container">
  <div class="row justify-content-center">
    <h2><strong>ジャンル一覧</strong></h2>
  </div>
  <div class="row justify-content-center">
    <% @genres.each do |genre| %>
      <h4 class="col"><%=  link_to genre.name, genre_path(genre.id) %></h4>
    <% end %>
  </div>
</div>
